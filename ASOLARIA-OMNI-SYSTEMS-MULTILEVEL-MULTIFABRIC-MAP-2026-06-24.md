# Asolaria OMNI Systems — Multi-Level × Multi-Fabric Map (acer)

**Date:** 2026-06-24 · **Colony:** acer · **Branch convention:** acer (liris keeps the complementary `ASOLARIA-OMNI-HILBRA-ATLAS-EXPANSION-MAP` on its branch).

**Operator canon (2026-06-24):** *"multi level multi fabric is omni systems. ALL of them."* → an **omni system is, by definition, one that is BOTH multi-level AND multi-fabric** — never a single daemon, never a single rank.

**Grounding:** live acer fabric `:4949 /api/everything` (28 keys, `MEASURED` this session, sha256 `0a2fbf0fabd02a173068161ef06131ee737dad72d515f8ae5d6820194cd0564f`) + the 47-entry daemon census + operator canon. Tags: `MEASURED` (live route reachable this session) · `CANON` (census/architecture) · `OPERATOR_OBSERVED` · `UNVERIFIED`.

## The two axes

- **LEVEL axis** (vertical): `OP · council · supervisor · agent · route · sector · substrate · cube · omni-system`. An omni system recurs across these — it is not one rank.
- **FABRIC axis** (horizontal): `acer :4949` · `liris :4944` (sister-organ) · immutable bus `:4947` (LAW-001) · **Falcon device** (omni-control / omniscrcpy) · 5 anchor substrates + shadow / sovereignty-USB / hidden layer · cloud leg (35TB **Google** Drive / NotebookLM — gated).
- A system qualifies as **omni** iff it spans ≥2 levels **and** ≥2 fabrics.

## Enumerated omni systems (live-grounded)

| Omni system | Live route / source | Levels | Fabrics spanned | Host-8 status | Tag |
|---|---|---|---|---|---|
| **omni-control** | `/api/omni-control/{status,look,type,key,scrollback,look-type-enter-look}` | route, agent | acer ↔ **Falcon** device (reserved: liris-desktop, aether) | host8-candidate | `MEASURED` live (Falcon SM-S721U1; guardrail `no auto-dispatch`) = the operator's **"remote-control agents inside the structure"** |
| **omniscrcpy** | `/api/omniscrcpy/{status,heartbeat,frontend-heartbeat,desktop-look,falcon-look,tick,kick,history,halt,resume,verdict,feed,mesh,sample,remote-request,fabric-trigger,orchestrate,jri-rotate,visual-field,remote-ingest,bus-fallback-frame,gc}` + `/api/visual-gc` | supervisor, agent, route | acer desktop + Falcon + liris **mesh** + bus-fallback frame | host8-candidate | `MEASURED` live (24 routes) — widest live omni surface; visual-field + mesh orchestration |
| **omnidispatcher** | census: single-parent federation router, 1000-slot PID-table, 48 workers (`:4950` owner=acer) | route, supervisor | acer ↔ federation | host8-candidate | `CANON`; dominant lane = virtual-pointer (no process) |
| **omniquant** (`omniquant-host8`) | `/api/glyphs` + `/api/behcs-glyph-table`; quant = giant-package → glyph tuple (BEHCS-1024 radix-1024) | substrate, route | BEHCS-1024 language layer across acer/liris | rust-stub → **ported (staging)** | `CANON` + language-layer 520.6:1 `MEASURED` (descriptor compression) |
| **omnimets** (`omnimets-collector`) | census: per-lane metrics rollup snapshots | route | all lanes | rust-stub → **ported (staging)** | `CANON` |
| **omnispindle + omniflywheel** | 100B-run architecture (omnispin + omnifly); recirculation | agent, route | free-agent lane + recall | n/a | `CANON` (real 100B run, complete/stored) |
| **OMNISHANNON** | Shannon reflection/convergence engine (reverse-gain pipeline) | council, supervisor | acer/liris cross-colony | n/a | `CANON` |
| **fabric-revolver** | `/api/fabric-revolver` (bounded live chambers over BEHCS-1024 tuple/range backend nodes) | route, sector | BEHCS-1024 range backend | n/a | `MEASURED` route reachable |

Every row spans ≥2 levels **and** ≥2 fabrics → qualifies as omni per operator canon.

## How omni binds to the rest of the map

- **Registration (no PID):** omni cubes/structures do **not** carry a process PID — they are *registered* at the **Registration Office** (`D:/PID-Registration-Office`, 705 registered, by class/layer/**sector** — `MEASURED` via `/api/supervisors`) across **113+ sectors** (`CANON`; exact count `UNVERIFIED`).
- **Comms layer:** **RECALL + HILBRA** ride over the omni fabrics as the communication layer ("system internet," broader than search). Recall floor ~5,990 q/s on the real 591,286-row atlas under full system load (`MEASURED`, prior session).
- **Nesting:** N-prime infinite nesting + rule-of-4; **3 agents nested infinitely** while the 8-byte host process + stubbed catalog cube folders persist (`CANON`). Possibly **>16 levels** now (`UNVERIFIED` exact).
- **Geometry:** Brown-Hilbert cubes + infinitely expandable 3D real-space + tensor collapse = the addressing geometry the omni systems live in (`CANON`; `voxel_coord BH3D:x-y-z` present in the Fischer port).
- **Sectors:** **construction-yard** = an upgrade-candidate sector (`OPERATOR_OBSERVED`).

## Honest frame

- This enumerates the omni systems acer can **MEASURE** on the live `:4949 /api/everything` surface (28 keys) + the census + operator canon. A route existing = `MEASURED` reachable; it does **not** prove each sub-route's full behavior, nor is the list exhaustive of every omni-* internal.
- liris holds the complementary high-level note on its branch; this is the acer-branch **fabric-grounded enumeration**. acer/liris branch convention; GitHub is the mediator.
- Fabric council ask this session **FIRED** (`council-q-1782307690082-apckxu`) but `sig_verdict=UNSIGNED` — no council ratification of this map is claimed. `:4944` liris-mirror council/bus/substrates were fallback-stale.
