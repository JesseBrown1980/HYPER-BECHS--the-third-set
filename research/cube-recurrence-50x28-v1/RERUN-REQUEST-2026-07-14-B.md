# Cube recurrence 50×28 — fresh rerun request B

**Date:** 2026-07-14  
**Purpose:** execute the complete merged 50-container `8 + 10 + 10` recurrence floor again from a new pull-request seat.

The run must return:

```text
50 base-cube receipts
8 reversible views per eligible cube
10 black/white Fischer viewpoints per eligible cube
10 exact recurrence passes per eligible cube
all byte/state gates
aggregate winner map
observed runner concurrency
```

The result will be compared against the two prior clean seats. Public inputs that are not pinned may legitimately change; every lane must retain its own source commit or digest. The private-input gate remains held unless its declared corpus is present.
