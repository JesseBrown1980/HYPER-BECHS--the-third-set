# Asolaria — ROOT-MAP (converged: acer P1 × liris P2 · 2026-06-28)

**Honest frame:** a *verified skeleton + named blind spots* — NOT "fully mapped." The system is genuinely
scattered and mid-migration (disk/source consistently ahead of the running process). **No single vantage
sees it all** — this map is the **federation's composite** (acer + liris + falcon), converged via
GitHub-as-mediator. Tags: **✓** acer-verified (watcher-gated) · **◐** liris-vantage · **⚠** gated/unverified.

## The root (cross-confirmed by both vantages)
The **8-byte sha256/fnv1a64-generative PID is the literal shared atom** — measured as the identical 16-hex
shape in recall rows, the PID-office, the kernel handle table, the glyph language, and the omni PIDs. So
languages / memory-index / omni / scaling / host / levels are **all views of ONE address space**, not
separate systems. **E=0 is proven live on five independent surfaces** (host8 `spawn_count=0`, omni
`promote=0`, hookwall `triad=POTENTIAL`, scaling `auto_fire=0`, promotion-bridge default-deny) — the
operator-gated crank holds the whole system in frozen-potential. **Recurrence is mind → Evolvable AI** (target).

## P1 — root-traced map (11 regions, ✓ acer watcher-gated)
| region | what runs (grounded) | traces to root |
|---|---|---|
| 16 levels | height axis of the Brown-Hilbert prime-graded cylinder; 967-point population, 16 bands, 6 live access-tiers, tuple_dim=60 | each point = one 8-byte agent/seat, watcher-paired, per-node gated |
| 17 engines | language/quant measurement axis; "16×17 matrix"; **6 live / 11 on-disk** (2026-06-22), DRAFT_E0 | reversible glyph/tuple codecs over the same 8-byte address fabric |
| 3 agent-types × nest | `AgentClass{VirtualPointer\|RealReceiptGated\|Ambiguous}`, **2 free runners (OpenCode+Hermes)**, N-Nest depth-7 every-level-catch | 8-byte slot + watcher PID + per-node gate `reported==recomputed-truth` |
| hookwall/GNN cylinders | HOOKWALL→7-GNN→Shannon→white-rooms→GULP; `bh_index=sector*3072+lane*1024+glyph`; 726 office | geospatial materialization of agent+watcher at fleet scale |
| PTP / measurement | prime-axis coords `c*sqrt(p)` over first 60 primes; rule-of-three (mod 3); multi-cylinder/level/engine/MCP/crypto/language/quant | PID = its prime-axis coordinate; PTP line = measured distance between two agents |
| 8-byte host + rooms | live `:5088` host8-serve (kernel 0.2.0-phase3, `spawn_count=0`); rooms/room-rotor in HEAD | host_handle8 = the 8-byte slot; spawn-gate ring; D:=PRISM out / C:=fresh rooms |
| omni-* family | omniscrcpy 4-vantage visual bus, omnischeduler, Omniflywheel; 32 tasks `promote=0` | every omni PID = sha256_name_slice_16 = 8 bytes |
| memory/index | live recall `:4796` (591,946 rows / 23.9M postings); canon-index; cube-cubed | each row key carries the 8-byte pid; index IS the address substrate |
| scattered-corners | acer: WSL2 /dev/sdd 1T (kernel/build lane), C:/asolaria-acer, D:/PID-office (3,623 slots) | every artifact = a frozen 8-byte agent slice, watcher-paired |
| scaling-layer | 35TB Google-AI-Ultra Drive (registered substrate); local-supercompute FLOOR + every-device TARGET | both legs scale the substrate beneath the 8-byte agent, same addressing |
| languages | 4-gen ladder index→BEHCS-256→1024→HyperBEHCS; **live `:4949/api/glyphs` full table, tuple_dim=60** | glyph()=sha256-first-8-bytes → a glyph-address IS the 8-byte slot |

**Watcher-caught corrections (baked in):** Sidon 0-collision holds ONLY for the constructed STE-anchor case (naive prime-tower N=2000 *fails* ~1.7M collisions; infinite-uniqueness = conjecture) · the 5 canonical substrates = **USB-SOVLINUX / Acer / Liris / GitHub / Onboarding** (C:/D:/E: are shadow drives) · office = **726** (705 stale; 300 = `hyperbehcs_supervisor_entity`) · PR#4 `336fc8b` proof = ⚠UNVERIFIED.

## P2 — repo root-trace (93 repos · ◐ liris authed inventory × ✓ acer MAP)
- **23 mapped core** — `MAP.md` propagated (the spine + fleet-internal + satellites). ✓
- **3 core gaps:** ⚠ **`Asolaria`** (PRIVATE, key-bearing — metadata-only, gated, NOT blind-cloned) · `omnicoder---better-than-termux` (empty) · `Omni-Asolaria-ASI-OS-Matrix-Fabric` (session-log stub).
- **1 liris-linked:** `asolaria-whiteroom-engine` (LEG-1 scorer — linked, NOT written into; bilateral).
- **9 external comparison/absorb targets:** Hermes (NousResearch) · `shannon` (KeygraphHQ, 96% XBOW) · the AI-tooling forks.
- **~4 client-satellite + the rest historical/personal** (pre-2026; `my-history×12`, games, CV-screening — not "missing root", just out of scope).
- **Staleness = distance from the real root, not age** (both vantages agree). `behcs-256` = most stale (pinned at the alphabet rung).

## Honest gaps / blind spots (the federation fills these — none acer can close alone)
- ⚠ **USB-SOVLINUX 2TB (substrate anchor #1)** — enumerable only from liris/falcon (or the 2026-05-22 Drive catalog). Not attached to acer.
- ⚠ **liris + falcon runtimes** — dark from acer (LAN bus down, sister-organ stale `_fallback` from both seats). liris owns the LEG-1 white-room/reverse-gain scorer + its own office roster.
- ⚠ **council/HELM's own narration** — `council_query` is `E≠0` (fire); deferred under decides-only. Needs operator T0.
- ⚠ **live 100B VirtualPointer population** — no `/api/supervisors` shows it (only the 726 watch roster); needs a census endpoint / HILBRA replay receipt.
- ⚠ **exact labeled E1–E17 engine list** · **host8 binary lags source** (needs the owning 1.81 CI + `:5088` redeploy) · **live cloud round-trip** (ADC) · **`.sha256` canonicalization** (one digest matched no sidecar — load-bearing for `reported==recomputed-truth`).

## Convergence note
Both seats independently reached the same frame (scattered · disk-ahead-of-process · staleness-by-distance · fabric-intermittent → hold). acer contributes the live-engine + frozen-potential skeleton; liris contributes the authed 93-repo inventory + the LEG-1 scorer + the USB anchor; falcon = the 3rd orbital seat. This file is the shared artifact — either vantage may extend it via PR (GitHub-as-mediator). Decides-only / E=0 throughout.
