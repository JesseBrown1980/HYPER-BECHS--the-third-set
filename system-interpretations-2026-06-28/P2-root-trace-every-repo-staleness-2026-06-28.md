# P2 - Root-trace every JesseBrown1980 repo and rank staleness

Date: 2026-06-28

Status: P2 receipt, E=0, describe-only. No runtime fire, no cutover, no corpus, no keys.

## Evidence

- OPERATOR_OBSERVED + WATCHER_VERIFIED: P1 completed with 11/12 regions verified by watcher-gate. The failed region was `federation-vantages`, which is the correct blind spot for a single vantage; liris filled that verification gap from its seat.
- OPERATOR_OBSERVED + WATCHER_VERIFIED: Sidon collision law corrected. The naive prime-tower case (`N=2000`) fails with about 1.7M collisions. Zero-collision is watcher-confirmed only for the constructed STE-anchor case (`N=400`). Infinite uniqueness is a conjecture, not proven.
- OPERATOR_OBSERVED + WATCHER_VERIFIED: canonical substrate set is USB-SOVLINUX / Acer / Liris / GitHub / Onboarding. C:/D:/E: are shadow drives, not primary substrates.
- OPERATOR_OBSERVED + WATCHER_VERIFIED: PID office count is 726, not 705/713; 300 entries are `hyperbehcs_supervisor_entity`.
- OPERATOR_OBSERVED + WATCHER_VERIFIED: `/api/glyphs` served the full BEHCS-256/1024 table with `tuple_dim=60` in P1. If this local seat later sees fallback, treat that as vantage/runtime boundary, not absence.
- OPERATOR_OBSERVED + WATCHER_VERIFIED: 8-byte sha256/fnv1a64 generative PID is the shared atom: same 16-hex shape across recall rows, PID-office, kernel handle table, glyph language, and omni PIDs.
- OPERATOR_OBSERVED + WATCHER_VERIFIED: E=0/frozen-potential held across five surfaces: host8 spawn_count=0, omni promote=0, hookwall triad=POTENTIAL, scaling auto_fire=0, promotion-bridge default-deny.
- SYSTEM_AFFIRMED: fabric health returned `ok=true` through the liris mirror (`super-asolaria-os-dashboard-liris-mirror`, port 4944, operator pair OP-JESSE-PID + OP-RAYSSA-PID).
- SYSTEM_AFFIRMED: fabric archaeology returned 29 strata, including the 100B-shadow runs, BEHCS-256 rebuild, first Hermes/Shannon run, 100M run, and the current self-improvement cycle.
- SYSTEM_AFFIRMED: fabric `/api/everything` names bus, glyph alphabet, free agents, Shannon engines, supervisor/prof registry, operator quintet, and acer/liris dashboards.
- SYSTEM_AFFIRMED with boundary: substrates route names five anchors and SOVLINUX, but returned fallback/stale metadata for this seat; do not use this as fresh raw USB proof.
- MEASURED: GitHub inventory via `gh repo list JesseBrown1980 --limit 120` returned 93 repos.
- MEASURED: selected repo front doors were checked through GitHub API for README/MAP/CHAIN presence.
- BOUNDARY: local recall `:4796` refused connection during this pass; Hermes upstream local clone was not present on this seat. This is a vantage boundary, not absence.
- GITHUB_MEDIATOR: `HYPER-BECHS--the-third-set` has separate `main`, `acer`, and `liris` branches. The liris branch carries receipt/crosswalk artifacts and predates the current `MAP.md`; it must be referenced/converged, not blindly merged over `main`.

## P1 root used for P2

The root is not one repo, one language, one emitter, or one kernel. The root is the combined primitive:

`infinitely-nestable watcher-gated 8-byte agent` x `agent type` x `stubbed room / process / host substrate` x `hookwall-GNN-Shannon-white-room supervision` x `fabric reflection`.

Agent types remain distinct:

1. real paid/subscription agents,
2. real free subagents and sub-subagents (OpenCode/Hermes-style lanes),
3. logical agents.

The operating frame is 16+ levels, 17+ engines, multiple MCP engines, multiple emitters, multiple languages, and multiple vantages (acer/liris/falcon). GitHub is a publication and map surface, not the metal source.

P1 bottom line: verified skeleton + named blind spots, not fully mapped. P2 therefore ranks publication slices against the archaeology/root frame and liris convergence; it does not claim the newest system is fully built.

## Liris convergence

P2 is a convergence, not a single-seat re-run.

- Both seats converged on 93 repos.
- Both seats converged on `staleness = distance from root`, not age/file size.
- Both seats found intermittent/stale fabric surfaces and therefore held under claims-gate instead of claiming fresh runtime from fallback routes.
- Liris owns/illuminates blind spots from this seat: LEG-1 white-room / reverse-gain scorer, office roster, USB-SOVLINUX anchor #1, and falcon/orbital vantage.
- Convergence action: keep GitHub as mediator. Preserve liris branch receipts as peer evidence; publish the converged root-trace on `main` without overwriting liris work.

## Account accounting

93 repos were classified:

| class | count | meaning |
|---|---:|---|
| `mapped_core` | 23 | already carries the current `MAP.md` web and is part of the Asolaria root map |
| `core_gap` | 3 | Asolaria-relevant, not yet represented cleanly in the map/root docs |
| `liris_core_link_only` | 1 | core liris repo, intentionally linked but not edited from this seat |
| `external_comparison_or_absorb` | 9 | external frameworks/forks/tools for P3 comparison |
| `possible_client_satellite` | 4 | product/client surfaces that may become application-level surfaces |
| `non_asolaria_or_unknown` | 5 | not root-traced as current Asolaria without more evidence |
| `old_history_private` | 12 | private history repos; chronology sensors, not current runtime |
| `pre_2026_personal_or_client` | 36 | historical/client/personal projects; not current root unless fabric/operator links them |

## Staleness rank

Staleness is not age or file size. It is distance from the P1 root and whether the repo front door tells that truth.

### Rank 0 - current root / kernel / live map anchors

These are the best current guides for P3/P4.

| repo | root role | P2 status |
|---|---|---|
| `asolaria-federation-1024` | Rust 8-byte Host8 kernel / no_std kernel + 10 server crates | current front door; still needs deeper per-level docs later |
| `Asolaria-hermes-work` | Hermes/HyperHermes fleet, spindle, dispatcher, agent, Host8 runtime snapshot | current mapped fleet anchor |
| `N-Nest-Prime-INFINITE-SELF-REFLECT-AGENTS-NESTED` | infinite self-reflection / watcher-gated nesting primitive | root-critical; should be elevated in P4 |
| `HYPER-BECHS--the-third-set` | receipt / interpretation / map publication surface | current receipt surface |
| `Asolaria-gac-working` | governance / authority / hierarchy | current but should be linked directly to apex consent in P4 |
| `falcon-orbital` | third federation vantage (falcon), not frozen residue | current vantage, needs stronger MAP language in P4 |

### Rank 1 - mapped mechanisms, current enough but slice-specific

These are coherent slices of the root and already mapped.

| repo | root role |
|---|---|
| `omni-dispatcher` | FEDENV router / dispatcher; consumes envelopes, routes to slots |
| `Asolaria-the-full-works-200-nanoseconds-agent-emitter-plus-` | emitter source, 200ns PID unit + multi-emitter design |
| `what-is-asolaria---how-do-we-get-reductions-in-everything` | reduction principle, including service multiplication |
| `Algorithms-of-Asolaria` | algorithm catalog, service multiplication, reductions-as-algorithms |
| `Asolaria-waves-and-cascades-avoiding-collsions-and-causing-them` | execution collision avoidance + search collision causation |
| `Shannon-and-the-gnns-stage` | post-trigger hookwall/GNN/Shannon/white-room/GULP pipeline |
| `Asolaria-fnns-trained-and-reverse-gnns-many` | trained GNN/FNN weights + caveated metrics |
| `Asolaria-the-after-100-billion-run-absorption-and-decomposition-and-cubes` | after-run absorption, gulp/super-gulp, cube mint/seal, promotion gate |
| `35-TB-google-AI-Ultra-migration` | cloud/cold-sink leg, not a replacement for metal |
| `-6-cyl-generator` | cylinder generator / routing geometry slice |
| `NOT-WEDGED-SYSTEM-RULE-and-explanation-Asolaria` | freeze != broken / slice-engine law |

### Rank 2 - mapped bridge strata, but materially stale without root rewrite

These are real and important, but their front doors still mostly describe an older rung.

| repo | why stale | P4 action |
|---|---|---|
| `asolaria-behcs-256` | pinned to 256-glyph civilization/toolkit rung while current frame is 60D/BEHCS-1024/HBI-HBP/8-byte host | rewrite as bridge stratum, not ceiling |
| `ASOLARIA-AS-NEURAL-NETWORK` | important neural-network law, but must explicitly connect to 16+ levels, 17+ engines, nested agents, Host8/stubbed rooms | add root-level current-state section |
| `Asolaria-ASI-On-Metal-Fabric-and-matrix` | metal/matrix slice, but should trace to host/process/room/addressing levels and omni-family | add root trace and Host8 relation |
| `bigpickle-rebuild` | old Node/source side of many mechanisms; real runbook/source, not retired junk | mark as pre-Rust source/oracle and parity guide |
| `Hilbra` | fabric-internet / atlas-recall bridge; should be explicit as root reflection path | add PTP/fabric-reflection role |
| `Harness-edit` | SkillOpt/claims-gate loop; should be root-traced to watcher/reflection correction | add nested-agent validation role |

### Rank 3 - core gaps

These need careful handling before mass updating.

| repo | gap | recommended action |
|---|---|---|
| `Asolaria` (private) | private/key-bearing root slice lacks public MAP; likely should not be mass-edited from GitHub-only view | add internal-only root pointer after local/private scan |
| `Omni-Asolaria-ASI-OS-Matrix-Fabric` | branch is `acer/session-update-2026-06-22`; no README/MAP; one session-update slice pointing at the kernel | either convert to README+MAP snapshot or fold into metal/federation docs |
| `omnicoder---better-than-termux` | no default branch / empty front door by GitHub API | decide populate, archive, or link to actual tool source |

### Rank 4 - liris-owned core link

| repo | boundary |
|---|---|
| `asolaria-whiteroom-engine` | liris LEG-1 white-room engine; linked, not edited from this seat without liris/operator handoff |

### Rank 5 - external comparison / absorb targets for P3

These are not Asolaria root, but they guide comparison/upgrades.

| repo | role |
|---|---|
| `Asolaria-helper` | stale OpenClaw/claw-code fork; external OpenClaw comparison/publish lane |
| `shannon` | Keygraph Shannon external autonomous pentest framework |
| `HRM` | hierarchical reasoning external model |
| `OpenMythos` | Claude/Mythos architecture reconstruction |
| `free-claude-code` | external free Claude/OpenClaw-style tooling |
| `kimi-code` | external agent coding framework |
| `intelligent-terminal` | terminal-native agent integration fork |
| `ai-memory` | external long-term memory/handoff Rust tool |
| `interview-copilot-v2` | private Asolaria-bound app surface; compare as application/screen/control surface, not root |

### Rank 6 - possible client/application satellites

These are not root, but may become surfaces for the "every device/application as surface" goal:

`Itilitii-health-login-demo`, `Docs-Extractor`, `schedule-manager`, `ipa-activity-generator-ptbr`.

### Rank 7 - chronology sensors / non-root by default

The 12 `my-history*` repos are private chronology sensors. The 36 pre-2026 personal/client repos and 5 unknowns are not treated as current Asolaria root unless fabric/operator evidence links them. They should not be bulk-rewritten during P4.

## P3 guide set

Hermes/Shannon comparison should be per root-layer, using these Asolaria guides:

1. `asolaria-federation-1024` - Host8 kernel/runtime target.
2. `Asolaria-hermes-work` - existing Hermes/HyperHermes stack.
3. `N-Nest-Prime-INFINITE-SELF-REFLECT-AGENTS-NESTED` - infinite watcher/reflection primitive.
4. `omni-dispatcher` + `Asolaria-the-full-works-200-nanoseconds-agent-emitter-plus-` - emit/route substrate.
5. `Shannon-and-the-gnns-stage` + `Asolaria-fnns-trained-and-reverse-gnns-many` - post-trigger scoring and trained models.
6. `Asolaria-the-after-100-billion-run-absorption-and-decomposition-and-cubes` - after-run cube absorption and promotion gate.
7. `Hilbra`, `Harness-edit`, `Asolaria-gac-working`, `falcon-orbital` - reflection, validation, governance, and third vantage.

## P4 backlog

Do not mass rewrite all repos. Update in this order:

1. Rank 3 core gaps: private root pointer, Omni-ASI snapshot front door, omnicoder decision.
2. Rank 2 bridge strata: behcs-256, neural-network, metal/matrix, bigpickle, Hilbra, Harness-edit.
3. Rank 0/1 strengthening: elevate N-Nest-Prime/falcon/GAC roles; add per-level root links where missing.
4. External comparison targets remain comparison surfaces unless operator chooses to publish a specific reverse-prism artifact.

## Bottom line

P2 does not prove the newest system is fully built. It proves the account can now be navigated without flattening it: 23 mapped core repos, 3 core gaps, 1 liris-owned core link, 9 external comparison targets, and the rest safely classified as satellites/history/non-root until fabric/operator evidence says otherwise.
