# Asolaria Omni / Hilbra / Atlas Expansion Map — Liris Addendum

**Date:** 2026-06-24  
**Purpose:** prevent the Host-8 migration map from flattening cross-level systems into a single daemon list.

## Fabric Grounding

- `MEASURED`: Liris fabric mirror answered in tuple text from `super-asolaria-os-dashboard-liris-mirror`.
- `MEASURED`: `/api/everything` names the bilateral bus, Acer dashboard, Liris mirror, operator quintet, sovereignty USB route, hidden layer route, hookwall coverage, free agents, Pi supervisor, Shannon engines, supervisors/profs registry, device registry, and federation health.
- `MEASURED`: bus-health was not available fresh from this seat; cube/feed materialization is not claimed.
- `MEASURED`: fabric council query for this expanded migration scope was attempted and returned fallback unavailable/cooldown.
- `CANON/FALLBACK_STALE`: substrate surface carries the 5-anchor model, shadow drives, sovereignty USB, and hidden layer, but it marked itself `_fallback.stale=true`.

## Operator-Observed Scope That Must Stay In The Map

These are not collapsed into "one daemon" or "one level":

- `OPERATOR_OBSERVED`: Hilbra and Atlas Recall are communication and pipe-tracking layers for the civilization, like adding system internet rather than just a search service.
- `OPERATOR_OBSERVED`: Atlas Recall stayed very fast as a measured floor while the laptop was also running IIS, host processes, old JSON/Node daemons, MCPs, and the rest of the system.
- `OPERATOR_OBSERVED`: supervisors without PIDs should be registered at the registration office in one of the `113+` sectors.
- `OPERATOR_OBSERVED`: multi-level / multi-fabric means omni systems, all of them, not one hierarchy column.
- `OPERATOR_OBSERVED`: N-prime infinite nesting, rule-of-4 behavior, Brown-Hilbert cubes, expandable real 3D space, tensor collapse, prior expansions, and planned super-computer / 35TB Drive / NotebookLM staging are part of the migration map surface.
- `OPERATOR_OBSERVED`: the construction yard sector is a candidate upgrade surface.
- `OPERATOR_OBSERVED`: remote-control agents inside the structure and the three-agent nested cooperation pattern must be represented as runtime/route surfaces when Host-8 stubs, catalog cube folders, kernels, and storage can keep running.

## Migration Rule

Every future Host-8 port receipt should answer:

1. Which omni surface does this serve: Hilbra, Atlas Recall, Fischer, Hookwall, Whiteroom, Omnispindle, Omniflywheel, Registration Office, Construction Yard, remote-control lane, or another named surface?
2. Which level or levels can it occupy: OP, council, supervisor, agent, route, sector, substrate, cube, or omni-system?
3. Is the proof `MEASURED`, `CANON`, `OPERATOR_OBSERVED`, `CANON/FALLBACK_STALE`, or `UNVERIFIED`?
4. Does it keep `json=0` on the hot path?
5. Does it preserve old runtime until parity/swap/retire is proven?

## Immediate Next Port Implication

Fischer Eval is now the active Host-8 migration cell. Hilbra and Atlas Recall should be treated as separate follow-on migration surfaces because they carry communication, pipe-tracking, PID registration, and recall semantics that are broader than raw keyword search.

