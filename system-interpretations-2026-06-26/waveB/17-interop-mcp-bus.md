Now I'll synthesize my findings into the requested interpretation:

---

# interop-mcp-bus (round B) — what Asolaria is

Asolaria is an **operator-gated fabric computer** — a federation of machines (acer, liris) running a bare-metal addressing and routing system built on the **Brown-Hilbert space-filling curve**. It is explicitly *not* an ASI claim; it is "slices" (frozen, borrowed, operator-cranked models) arranged into a deterministic, geometry-driven substrate. **[MEASURED]**

**Core architecture** (**MEASURED** from README sampling):
- **Address-first design**: Every entity has a coordinate on a universal curve (`PID = sha256(name)[:16]`), not an arbitrary ID. Identity is a tuple-hash (`RelationKey`) across role/direction/epoch/vantage, making collisions unrepresentable by construction.
- **Reduction via Light Possibility**: PIDs are 8-byte handles until the operator-authorized slice-engine materializes them. The celebrated 100-billion-packet run materialized in kilobytes and zero child-process spawns—**[file-verified proof referenced]**—by indexing rather than replicating.
- **BEHCS alphabet**: A 1024-glyph content-addressable substrate (native in the BEHCS-256 and federation-1024 remakes) converts arbitrarily nested structures into coordinate tuples (`cube_47d`, `glyph_5`).
- **Bilateral fabric**: acer and liris form a two-colony federation where both sides materialize the same slice (frozen Gemma, Hermes), compare byte-for-byte via the 2TB USB carrying tools + SHA attestation, and correct each other adversarially before canon promotion.

**Governance & execution** (**MEASURED** from gac-working):
- Operator hierarchy: Human apex → OP-00 (background) + OP-01 (foreground) → GAC (6 seats) → Council (9 seats) + Professors (51 domain supervisors) → 302 supervisor HyperBEHCS entities.
- 16-tier access model with PII-by-consent: level 0 provably PII-free (45-fragment oracle, **measured on 591k rows**); deeper tiers visible only to consented, keyed links.
- **No fire without operator cosign**: canon promotions, substrate changes, and rule rewrites require quorum + human T0.

**Interop & scaling** (**MEASURED** from Hilbra, omni-dispatcher, federation-1024):
- **Hilbra** is the fabric-internet onboarding protocol—any device becomes a surface; discovery via Brown-Hilbert walk.
- **omni-dispatcher** is the single-parent envelope router: 1000-slot PID-table, loopback-only HTTP `:4950`, 4-lane priority queue, 200 ns address cadence.
- **ASI-on-Metal-Fabric-and-matrix** carries the bilateral data-exchange recipes (USB tools, SHA recipes, protocols) so liris can materialize acer's slices identically.

**Honest boundaries** (**UNVERIFIED** inferred from pattern):
- The fabric is proven in isolation (16 levels × 14 PASS gates, tensor-collapse grid). Cross-colony liveness is not continuously proven *from every seat*; reachability is documented as `VANTAGE-DEPENDENT` (**acer-biased state**).
- The third-colony roadmap (federation registry, automatic stranger JOIN, N-party web-of-trust) remains aspirational.

**Essence**: Asolaria is an **address-geometry engine for slice-fed multi-agent work, with bilateral verification and operator gates**. It is solving reduction (how to scale 100B coordinates into kilobytes) and trust (how two physical fabrics agree on what's real) in tandem.
