# governance-safety (round B) — what Asolaria is

**MEASURED:** Asolaria is a **bilateral, operator-governed system** orchestrating deterministic neural computation across two physical machines (acer x86, liris x86) + two mobile ARM devices (falcon, aether), totaling ~72 concurrent agents. The substrate is **frozen brain slices** (Gemma-4B, cached deterministically on disk) + **borrowed LLM slices** (via lawful subscription "ordinary individual usage," not metered APIs). The system **explicitly disclaims ASI status** — it is "frozen + borrowed intelligence orchestrated into neural-network shape," wired together through five proven components (dashboards, omnidirectional translators, token↔cube bindings, N-Nest watcher gates, supervisor-suggestion pipelines).

**MEASURED:** Governance is enforced via **cosign chain** (append-only, signed by OP-JESSE-BROWN + four witnesses: Rayssa, Amy, Dan, Felipe, through 2026-09-23). Every live-touching action (fabric POSTs, mints, device writes) is **operator-gated** — draft tools cannot execute by construction. Claims are **tagged MEASURED/CANON/UNVERIFIED/OPERATOR**; deflate-gate and claims-gate are explicitly banned. Bilateral vantage isolation is doctrine: acer cannot see liris internals; shared state is origin-local receipts transported via GitHub + byte-verified, not the intersection of what both hosts see.

**MEASURED:** The addressing geometry is a **47D proven Brown-Hilbert lattice** (1024-glyph BEHCS alphabet; capacity ≈10^180). The kernel is Rust no_std, ≤16 syscalls, booting from USB. Hookwall (pre/post syscall hooks) sits one layer above; GNN inference layer provides graph-attention primitives. All files carry sha256 sidecars; cross-machine parity is byte-verified on every test run (18 real defects caught cross-vantage, zero merge conflicts).

**UNVERIFIED (current):** Cross-colony liveness — Hilbra documentation explicitly tags current reachability as VANTAGE-DEPENDENT; liris reads of acer return stale fallback. The Rust canonical recall node compiles but is held under bilateral review for parity hardening before cutover.

Human-operator-authorized. Honest about what is proven and what is aspirational.
