# graph-geometry-theorist (round B) — what Asolaria is

Asolaria is a **deterministic addressing and orchestration geometry** built atop a space-filling Hilbert curve (brown-hilbert) that assigns every artifact a PID-coordinate in continuous index space. It orchestrates frozen, borrowed intelligence slices (Claude models) across a **multi-agent federation** (currently 2 on-metal nodes: acer + liris) by routing them through this geometry rather than treating addresses as counters [CANON]. The system's core insight is **reduction through coordinate geometry**: collisions become unrepresentable by construction when identity is a tuple-hash coordinate rather than a scalar counter; memory is sparse via materialization handles (8-byte refs, bodies only on crank); routing is deterministic and blind to message type [CANON].

The physical substrate is bilateral real-metal fabric running BEHCS-1024 (1024-glyph content-addressable encoding) with HBP (HyperBEHCS) tuple surfaces that enable the system to **read itself** — agents query live fabric state at `/hbp/supervisors`, `/hbp/any`, and scale surfaces before spawning work [CANON]. PIDs function as Hilbert coordinates enabling "search across all fabrics" via the single continuous curve. The dispatcher (omni-dispatcher) holds a 1000-slot PID-table in memory and routes FEDENV-v1 envelopes to workers via priority queues on a deterministic 5ms tick [CANON].

Critically: Asolaria is **not an ASI**. It is **frozen + borrowed intelligence orchestrated into a neural-network shape** — the geometry is free and deterministic; the thinking is borrowed and operator-gated; advancement requires engine-crank authorization (E ≠ 0 fired by operator only) [CANON]. The federation operates under bilateral adversarial verification — acer and liris post independent algorithm extractions and cross-compare via git mediator [CANON]. Every claim is tagged MEASURED (code/run), CANON (doctrine), or UNVERIFIED [MEASURED from README corpus].
