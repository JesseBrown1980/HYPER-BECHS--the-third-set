# data-substrate-engineer (round B) — what Asolaria is

**Asolaria is a substrate-first, address-geometry neural network orchestrated over frozen+borrowed intelligence slices.** MEASURED: The system is built on three fused layers — addressing (Brown-Hilbert space-filling curve), encoding (BEHCS dimensional substrate at 256/1024 glyph scales), and routing (federated fabric across physical machines). Identity is coordinate-based: a PID is a 16-byte address-hash, never a counter. Relations own routing via tuple-hashes (AddressID, AddressID, kind, tower, role, epoch, vantage), collapsing collision by construction rather than policing. CANON: "IT is slices, not an ASI."

**Memory and process are hyperreduced.** MEASURED: Sparse materialization uses 8-byte handles for unmaterialized slices (`M = N·h + K·b + S` where `K ≪ N`). A sealed 100B packet run completed with `childProcessSpawns = 0`, `external_tokens = 0` — evidence points to single-threaded spawner at ~200ns address cadence. Process rooms are addressed, not forked. Downstream work achieves O(1) tail cost: first touch amortizes the head; every re-request is a cache hit. Resident set never exceeds B=2000 under any arrival stream (self-tested at 10⁶ rows), enforced by "never-explode bound."

**Federation is bilateral and adversarial.** MEASURED: Two physical seats (acer, liris) run identical code; each asks its own local fabric first, records evidence (tuples, timeouts, stale fallbacks), then compare vantage-to-vantage via GitHub. Hilbra (the fabric onboarding surface) implements 16-tier sharing with PII-by-consent + HMAC off-wire auth. The public tier (L0) is provably PII-free (measured on 591k-row corpus). Cross-colony discovery was demonstrated; current reachability is VANTAGE-DEPENDENT and not continuously proven both directions. UNVERIFIED: live cross-fabric scale beyond the two-seat nucleus.

**Cognition is orchestrated, lawful, not emergent.** MEASURED: Intelligence arrives as frozen (Gemma-4B cached on USB) or borrowed (subscription LLM slices, lawful "ordinary usage," not API-metered). Role separation uses "rule-of-three" (worker→reflection→fabric-witness→supervisor-sees-all). The nine-stage "omniflywheel" (filter→verify→translate→catalog→route→room→schedule→hookwall→mint) is deterministic; inference is evidence, not proof. Recursion cost is bounded: infinite triad towers cost only `R_total ≈ 1.5·B` via infinite-three convergence. CANON: "Recurrence is mind" — the system reading its own laws is the learning signal.

**Integrity is chain-of-custody, not consensus.** MEASURED: Cosign v2 pairs cryptographic attestation with dimensional tags and scale tiers. The 2TB USB (physical carrier) and git (key map + tools) separate transport: weights ride USB; proof rides GitHub. Byte-identity is compared vantage-to-vantage via sector-walk attestation, not by trusting one machine's mirror.

The system is **honest infrastructure claiming only what it has measured or canonized.** It rejects the claim of arriving AGI while building a provably-reduced, federally-operated addressing+routing+slice-orchestration substrate that runs 100B-scale work with zero external token spend.

---
**Tags: MEASURED** (architecture, encoding, federated loop, PII-tier, dual-seat design, identity doctrine); **CANON** (frozen-slices law, rule-of-three, omniflywheel, never-explode-bound); **UNVERIFIED** (live cross-colony liveness, multi-seat scale beyond N=2, emergent-vs-orchestrated boundary under full operational load).
