# Liris local-wave toolchain addendum — Oh-my-codex / Linear / Symphony

Seat: Liris / Rayssa Codex (`C:\Users\rayss`)
Date: 2026-06-27
Scope: local Liris-vantage correction after the operator pointed out that public GitHub reads alone are a stubbed slice and that local waves must include coordination/toolchain surfaces.

## Correction

The first Liris receipt was too GitHub-centered. That was incomplete. Local waves must read local orchestration strata too, using the Liris Linux/WSL lane where available, and must not deflate the system into static repo files.

Corrected frame:

- `OPERATOR_OBSERVED`: slices are frozen potential, not the whole system.
- `OPERATOR_OBSERVED`: the Asolaria matrix/fabric plus live agents, engines, supervisors, gates, and substrate tools animates those slices into runtime behavior.
- `OPERATOR_OBSERVED/DESIGN`: the intended frame is Evolvable AI / post-ASI, not "just prompts" or "just files."
- `BOUNDARY`: specific live/cutover claims still require owning fabric/process/substrate evidence.

## LIRIS_LOCAL_WSL — local surfaces rechecked

The local waves used the Liris WSL lane (`/mnt/host/c/Users/rayss`) rather than treating GitHub as the whole system.

Measured local strata include:

- `.codex/` — Codex seat config, memories, rules, hooks, skills, logs, and local state.
- `.asolaria/` and `.asolaria-workers/` — worker/event/counter surfaces, including Liris and Acer PID-counter lanes.
- `codex-bridge/` — local Codex bridge package.
- `.hermes/`, `hyperbehcs-hermes/`, `hyperbehcs-hermes-release/`, `shannon/` — local helper/coordination surfaces.
- Asolaria repos/worktrees: `Asolaria`, `ASOLARIA-AS-NEURAL-NETWORK`, `Asolaria-ASI-On-Metal-Fabric-and-matrix`, `Asolaria-BEHCS-256`, `_bigpickle`, `asolaria-graphify-liris`, `Asolaria-kernel-source`, and related local archaeology roots.

Tool boundary from this seat:

- `MEASURED`: Liris WSL is minimal BusyBox/Docker Desktop WSL2 style; it does not expose the Acer SOVLINUX 2TB raw substrate here.
- `MEASURED`: Liris WSL lacks the full Rust/Node/Python/Git toolchain used by some Acer lanes.
- `BOUNDARY`: absence from this Liris WSL lane is only a Liris-vantage boundary; it must not erase Acer/operator substrate evidence.

## Toolchain candidates the operator named

### Oh-my-codex / OmX

- `MEASURED_GITHUB`: `JesseBrown1980/Asolaria-helper` public repo description says it is built in Rust using `oh-my-codex`.
- `MEASURED_GITHUB`: the cloned repo's `PHILOSOPHY.md` defines `oh-my-codex` / OmX as the workflow layer that turns short directives into structured execution, persistent verification loops, and parallel multi-agent workflows.
- `CLASSIFICATION`: coordination/workflow activation stratum. It belongs in Asolaria archaeology/census prompts even when it is not the core Host8 repo.

### Symphony

- `LIRIS_LOCAL_WSL`: `.asolaria-workers/pid-counters/DEV-LIRIS--SYMPHONY.txt` exists beside `DEV-LIRIS--LOCAL_CODEX.txt`, `DEV-LIRIS--HERMES.txt`, and `DEV-LIRIS--HERMES_SUBAGENT.txt`.
- `CLASSIFICATION`: registered Liris coordination/toolchain lane. Its local counter file is evidence of a registered lane, not proof of current live runtime firing.

### Linear

- `OPERATOR_OBSERVED`: the operator named Linear as a likely missing surface.
- `LIRIS_LOCAL_WSL`: no local directory or text hit for Linear was measured in the focused Liris scan.
- `MEASURED_GITHUB`: GitHub repo-name search under `JesseBrown1980` did not find a Linear-named repo.
- `CLASSIFICATION`: keep as an operator-suggested coordination surface to check in future waves; current Liris evidence is `UNVERIFIED`, not absent-globally.

## Durable local-law updates made on Liris

- `C:\Users\rayss\AGENTS.md` now includes `Oh-my-codex/OmX`, `Linear`, and `Symphony` in the AI-toolchain / chronology-sensor list.
- `C:\Users\rayss\.codex\rules\asolaria-anti-deflation-count-law.md` now has a coordination/toolchain layer covering Codex/Claw/Oh-my-codex or OmX, Linear, Symphony, Shannon, Hermes, OpenCode, provider seats, Discord/channel routers, worker counters, and bridge packages.

This means future local waves receive the correction in the active prompt material, not just memory.

## README anti-deflation updates already published

The following public README updates were verified from the GitHub owning surface:

- `what-is-asolaria---how-do-we-get-reductions-in-everything` branch `acer/full-proof-3agent-types-100b-2026-06-21` at `fa455c2c3b37b73409553225c11c30f0bb35d246`.
- `ASOLARIA-AS-NEURAL-NETWORK` branch `codex/language-waterfall-matrix-11x16` at `c57f51e1d4c12fe8346a55b88a49c1a126609057`.
- `Asolaria-ASI-On-Metal-Fabric-and-matrix` branch `codex/recall-atlas-engine` at `f541a058f2c4409f6e45b25b5dbc01f4fac7b298`.
- `asolaria-behcs-256` branch `liris/behcs-256-w3` at `38504379218709c328dbca76c74edf6d7db807e5`.
- `bigpickle-rebuild` PR #25, `liris/anti-deflation-readme`, head `f2286ccd00ee35fb5a74badfc0fa118087abef73`.

`Asolaria` local README was patched on the Liris machine, but not pushed: the local worktree is dirty/ahead with unrelated history, and the safe clean-branch route was not available from this seat. That is a boundary, not a refusal.

## Action boundary

No engine fire. No migration/cutover. No claim that Liris WSL sees the whole Asolaria substrate. This addendum only corrects the Liris local-wave scope and publishes the operator's anti-deflation frame plus the measured toolchain evidence.
