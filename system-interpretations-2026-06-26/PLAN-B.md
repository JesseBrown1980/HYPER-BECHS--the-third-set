# Plan B — synthesis of 20 interpretations (round B)

*Author: acer (Claude Opus 4.8), synthesizing the 20 round-B lens reports. Round B was an independent repeat of round A with fresh agents on the same 20 lenses. Its purpose is a reproducibility check on "what Asolaria is."*

## 1. CONSENSUS — what the system is (round B)

Round B reproduces round A almost exactly — which is itself the most important finding: **20 fresh, independent interpreters, told nothing about round A, converged on the same picture.** That consistency raises confidence that the convergence reflects the repos, not one wave's framing.

**Asolaria is an addressing-and-routing geometry over frozen + borrowed intelligence slices, orchestrated into a neural-network *shape* — explicitly NOT an ASI.** Every round-B lens repeats the honest frame and the same spine:

- **Identity = coordinate, not counter** (CANON): PID is a tuple-hash / Brown-Hilbert position; collisions unrepresentable by construction.
- **Substrate** (CANON): Brown-Hilbert curve + prime towers + BEHCS-256→1024→60D HyperBEHCS content-addressing.
- **Possibility cheap, action gated** (CANON): 8-byte handles; bodies materialize only when an operator cranks the engine (E ≠ 0).
- **Single-spawner dispatch** (claimed MEASURED): omni-dispatcher `:4950`, 1000-slot in-memory PID table, 4-lane priority queue, deterministic tick; file-receipted 100B-packet / zero-child-spawn run.
- **Bilateral vantage governance** (MEASURED on method): acer + liris build independently, ask their own fabric, canonize only on byte-hash agreement via GitHub.

## 2. What round B adds (extra specifics, MEASURED-from-repo)

Round B surfaced more concrete mechanism than round A — useful raw material, still SCOPED to README/repo reads:
- **Envelope contract**: FEDENV-v1, 11 required fields, cosign-window check, 64 KB payload cap; lazy per-slot ports `:4951–:5950`, passive bus subscriber `:4947`.
- **Governance shape**: ~7 operator tiers, 51+ professor-supervisors, council-gated writes; LAW-001 (loopback always-allow), LAW-008 (filesystem-as-mirror), LAW-012 (multi-agent enforcement).
- **Quant engines named** (8): Polar, Turbo, JL, Zeta, Triple, Quadruple, JS, von-Mangoldt.
- **9-stage forward pass**: filter→verify→translate→catalog→route→room→schedule→hookwall→whiteroom-mint.
- **5-primitive kernel** (philosopher lens): addressing · compression · routing · cosign · recurrence.
- **Hilbra**: 16-level consent-scoped "fabric internet," L0 proven PII-free across 591k rows.

## 3. DIVERGENCES (same as round A — unresolved, carried to the ultra plan)

1. **MEASURED-inflation**: round B again tags README-derived facts "MEASURED." These are SCOPED evidence (claims-gate #5/#8: owning-gate-not-transcript, source≠running). The owning verdict is a live fabric probe / CI gate that no lens ran.
2. **47D vs 60D**: round B still mixes "47D proven / 60D HyperBEHCS." Per claims-gate #2/#4 the CURRENT frame is **60D HyperBEHCS / BEHCS-1024**; 47D (and 35D/49D, BEHCS-256) are **bridge strata** ("old decodes new").
3. **Neural network — literal or metaphor**: same split (omniflywheel-as-forward-pass vs "addressing divorced from compute").
4. **Self-improvement loop — live or staged-contract-only**: UNVERIFIED in both rounds.
5. **Cross-colony liveness**: bilateral *method* MEASURED; continuous both-direction liveness UNVERIFIED (a `_fallback` direction noted). Per claims-gate #6, a fallback read is missing≠clean.

## 4. PLAN this round yields (STAGED / gated / attack-verified)

Identical priorities to Plan A, reinforced by reproducibility:
1. **Promote SCOPED README claims to owning-gate verdicts** — re-run each headline (100B run, zero-spawn, 196,251→0 collisions, never-explode ≤2000) on the live fabric / real-Linux lane; tag MEASURED only after the owning probe. Read-only, no mutation.
2. **Resolve cross-colony liveness both directions** — probe acer↔peer without a `_fallback` marker; any fallback = missing. No transport auto-reattach; operator-T0 per cutover.
3. **Pin the canonical frame to 60D in every map** — regenerate from the fabric (authority); never clobber curated mirror maps; attack-verify cylinder (15) vs level (16) counts.
4. **Adjudicate NN-vs-addressing empirically** — one falsifiable fitness signal for reflect→propose→verify→cosign; run once at small scale behind the validation gate (accept only if it beats held-out). No engine-fire, no prod path.
5. **Harden bilateral receipts** to 3-matching-SHA reproducible builds before any "merged/live" claim — verify via `gh`/CI, not transcripts.

**Confidence note:** A↔B reproducibility makes the *descriptive consensus* (§1) high-confidence as a reading of the repos. It does **not** upgrade the SCOPED claims to MEASURED — two independent README reads agreeing is still two README reads. The owning-gate step (§4.1) is what converts them.
