# ontologist-data-model (round A) — what Asolaria is

**Asolaria is a frozen-intelligence-slice orchestration system** — not an ASI, but a **neural network shape built from borrowed + frozen brain slices**, orchestrated through deterministic addressing geometry and operator-gated advancement. It answers the question: *how do you get reductions in everything?*

## Essence (MEASURED)

The system is **tuples, not agents**. Every entity is addressed by tuple-hash (`RelationKey`), never scalar identity. A PID is a **coordinate in 60-dimensional BEHCS-1024 space** (~10^180 capacity per tuple), making collision impossible by construction. Possibility is dirt-cheap (8-byte handles); action is gated (operator authorizes `E≠0` cranking). The 100-billion-packet run proves this: 100B packets, zero child-process spawns, zero external tokens — a single 200 ns spawner routed everything through a 1000-slot PID dispatcher.

## Architecture (MEASURED + CANON)

Five regulators weave into a **live address fabric**:

1. **Prime/BEHCS geometry** — 60D tuple space over 1024-element alphabet; 16 vertical levels
2. **Rule-of-three (mod-3)** — worker/reflection/witness roles; supervisors see all three
3. **Brown-Hilbert + Sidon** — distance-unique projection (196k pairs → 0 collisions)
4. **Slice-engine cranking** — frozen Gemma-4B + borrowed LLM slices, lawfully subscripted
5. **Bilateral correction** — acer + liris vantage-gates each other; fabric-read-first doctrine

The **9-stage omniflywheel** is the forward pass: filter → verify → translate → catalog → route → room → schedule → hookwall → mint. GNN edges + self-reflect backprop. Every major claim is tagged MEASURED/CANON/UNVERIFIED; write authority stays operator-gated (cosign + vote-quorum + loopback bind).

## What it's becoming (MEASURED + UNVERIFIED)

A **self-improving neural network** observable by supervisors in real time. Reflect → propose → adversarially verify → cosign. The bilateral loop (acer/liris) proves it: each side reads its fabric, materializes artifacts on its drives, compares hashes, triggers correction before canonizing. No silent upgrades; no single machine is authority — only byte-hashed receipts transported through GitHub (current bridge) survive divergence checks.

Identity doctrine is absolute: distance never owns identity. `RelationKey` (tuple-hash) survives as long as the canonical preimage is stable; the distance certificate is scoped to one embedding version and re-proven on growth or dropped to DRAFT.

## Data model (MEASURED)

Sparse materialization: `M = N·h + K·b + S` (handles vastly outnumber materialized bodies). A 100-billion-packet corpus is stored in kilobytes (referential codebook, not lossless compression). Tuples, not JSON, on the hot path. Cubes: 256 ≤10-byte genius weights (raw→cube 1.93M×), content-addressable, infinite addressing. The 10k rooms + 113 sectors are canonical supervisor topology + HBP tuple registry; the 20k/50k/100k layers are logical/staged/catalog-backed.

**This is not the future of Asolaria—this is its present, file-verified and bilaterally gated.**
