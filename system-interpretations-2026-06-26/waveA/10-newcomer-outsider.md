Based on my examination of the Asolaria public repositories, here is my interpretation:

---

# newcomer-outsider (round A) — what Asolaria is

**MEASURED**: Asolaria is a deterministic, geometry-driven agent coordination system built on borrowed and frozen intelligence slices—not an ASI, but a frozen-brain orchestration layer shaped like a neural network. Its core thesis: reduce every dimension (memory, process cost, recursion depth, search space) by making addressing cheap and action gated.

**The architecture (MEASURED)**:
- **Frozen substrate**: Licensed intelligence subscriptions (e.g., Gemma-4B slices, LLM endpoints) are baked in as immutable network nodes, never updated during runtime. No LLM-inference hot path.
- **Geometric addressing**: Everything—agents, rooms, supervisors, relations—lives on a Brown-Hilbert space-filling curve. A PID is a *coordinate*, not a counter. Collisions become unrepresentable by construction. Identity is a tuple-hash (`RelationKey`), not scalar distance.
- **HyperBEHCS encoding**: A 60-dimensional addressing space (BEHCS-256/1024/HyperBEHCS) encodes 10^180 distinct addresses—unimaginably larger than observable matter. Cost: 8-byte handles; materialization only when work flows through a gate.
- **Multi-agent civilization**: Federated `asolaria-behcs-256` toolkit (39 packages, 23 runtime JS engines) orchestrates heterogeneous Claude-variant nodes (desktop, phone, remote) into a deterministic bus-and-supervisor topology.

**The law (CANON)**:
- No claim without [MEASURED/CANON/UNVERIFIED] tag and proof category
- Identity doctrine: distance never owns identity; a `RelationKey` is immutable under conditions (hash-stable tuple, domain-separated preimage)
- Operator-gated execution: every load-bearing claim tagged [EXISTS] (file on disk) or [NEW] (designed mechanism reduced to primitive)

**The bilateral verification (MEASURED)**:
Acer and Liris build from different seeds and cross-verify via GitHub. A 100-billion-packet run was executed with zero process spawns and zero forks—all state materialized once, deterministically. Proof file exists on disk.

**The inference (UNVERIFIED)**:
The system reads as *shaped like* a neural network—supervisors see all three (worker/reflection/fabric), frozen slices are "neurons," GNN edges are "synapses"—but no learning happens. Correction is operator-gated. This is deterministic replay of curated frozen intelligence, not emergent reasoning.

**In one sentence**: A coordinate-addressed, geometry-driven, frozen-slice orchestrator that makes distributed agency cheap by encoding everything on a universal space-filling curve and gating all mutation through operator consent.

---
