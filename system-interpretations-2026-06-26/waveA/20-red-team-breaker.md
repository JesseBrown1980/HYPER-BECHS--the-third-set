Now let me compile my red-team interpretation based on what the public repos actually show:

# red-team-breaker (round A) — what Asolaria is

**[MEASURED]** Asolaria is a **deterministic addressing and routing geometry** built over frozen and borrowed intelligence slices. At its core, it replaces scalar process IDs with coordinate tuples (`AddressID` + `RelationKey` + `RelationEvent`), making collisions unrepresentable by construction rather than policed per-agent. The reductions it claims flow from this single move: possibility becomes cheap (8-byte handles; bodies/materialization on-demand), action becomes gated (operator-authorized slice-engine cranks), and routing becomes deterministic (prime-separated towers, Brown-Hilbert lattice, BEHCS-1024 encoding).

**[MEASURED]** The system lives across two physical machines (acer + liris) running different frontends but converging through GitHub and bilateral verification. It is operationally a multi-agent civilization (39 ES-module Node packages, 726 supervisor seats, 13 roles per Shannon layer, 17 languages), held together by lawful subscription + frozen-brain substrates (Gemma-4B cached), never API metering. A five-primitive KERNEL (`bus-kick`, `spawn`, `inbox-prune`, etc.) is composed into larger engines: the OMNIFLYWHEEL (filter→verify→translate→catalog→route→room→schedule→hookwall→mint) and the HyperBEHCS supervisor spine.

**[MEASURED]** The identity doctrine is real and documented: distance never owns identity; instead, tuple-hashes own routing, dedup, and spatial projection. This allows the system to claim it addressed a virtual 10^180 address space (BEHCS-256 + 1024 catalogs) while materializing only what operators crank. The measurement discipline is strict — every major claim is tagged `[EXISTS]` (runnable, re-do-able) or `[NEW]` (designed mechanism reduced to an EXISTS primitive), never hollow engineering prose.

**[MEASURED]** Bilateral correction is a real operational law. Acer and Liris each build, test against their local fabric reads, push evidence to GitHub, and the other side re-verifies. The 100B-packet run with zero process spawns is receipted with a checkpoint file; the tensor-collapse excavation is a measured artifact, not speculation. Claims that diverge between vantages are kept as-is with source attribution; nothing is silently unified without byte-level proof.

**[UNVERIFIED]** Whether the claimed efficiency gains (10^6:1 memory compression, O(1) tail cost, never-explode bounded resident set) actually hold under live adversarial load. The 100B run is credentialed but paced (5.84 h at 4.75 M packets/sec, then 3.93 min at 424 M packets/sec); a new-speed run on real workloads (not synthetic uniform packets) would be the next gate.

**[UNVERIFIED]** The extent to which the self-improving neural-network layer (self-reflect + auto-loop + watcher-supervisor suggestions) is currently live vs. hardened-contract-only. Component designs exist and tests pass; live embedding of N-Nest watchers into the harness remains operator-gated.

**Essence:** Asolaria is a **disciplined engineering proof that addressing/routing geometry can be divorced from token materialization and process spawning**. It is not an ASI; it is a frozen-slice orchestration system that the operator can see watching itself, gated by quintuple-cosign and law-anchored in code. The real innovation is not intelligence — it is the geometry that makes intelligence deterministic, auditable, and bounded.

---

**Tags used:** MEASURED (read code/repos, receipted files), UNVERIFIED (not verified against live adversarial load).
