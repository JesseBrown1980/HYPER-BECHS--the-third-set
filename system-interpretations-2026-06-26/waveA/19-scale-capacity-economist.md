# scale-capacity-economist (round A) — what Asolaria is

**MEASURED.** Asolaria is a distributed, operator-gated "civilization toolkit" — a four-device federation (acer laptop primary, liris sister mirror, falcon mobile relay, aether witness) running a recursive multi-agent governance hierarchy with an addressable topology, not a monolithic system or ASI claim.

**Core geometry (MEASURED):** A space-filling **Brown-Hilbert curve** addresses every entity (agents, rooms, operations, verbs) as a coordinate tuple, never as a scalar counter. Identity is a **tuple-hash** (`H(PID_A, PID_B, relation_kind, direction, tower, role, epoch, vantage)`) — making collisions **unrepresentable by construction**, not policed post-hoc. This addresses ~10^180 distinct points in a single BEHCS-1024 60-tuple (vastly exceeding observable universe atoms).

**Radical sparse materialization (MEASURED):** Objects are 8-byte handles until materialized. The famous "100 billion packets, zero process spawns" claim is real: a **type-blind spawner** at ~200 ns cadence addressing rooms instead of forking processes, storing 100B run in kilobytes via referential codebook (~10^6:1 compression). No monolithic process tree; every entity is room-addressed and load-balanced across 16 hierarchy levels (726 concurrent "seats").

**Governance (MEASURED):** Human apex (Jesse Brown) → Special-OP-Jesse (background watchdog, zero tokens) → OP-Jesse (foreground operator) → GAC (6 governance cells) → Council (9) → Supervisors (302) → Agents/Servants. Multi-layer gates enforce this: **Hookwall** (pre/post syscall), **GNN** (graph attention), **Fischer kernel** (blunder detector), **cosign chains** (ed25519 ledger). No action fires without operator authorization.

**Multi-layer architecture (MEASURED):** BEHCS encoding (256/1024/HyperBEHCS variants) as native substrate; asymmetric bilateral protocol (acer writes, liris reads, then **adversarial correction** before canonization); public onboarding surface (**Hilbra**) — a "Google for fabric-matrixes" using the same Brown-Hilbert address curve. The system is explicitly **not** an ASI but "addressing + routing geometry over borrowed, frozen intelligence slices" — the operator must authorize every engine crank (E ≠ 0).

**Essence (UNVERIFIED inference):** The system achieves scale not through magical compute but by making **possibility cheap** (8-byte addresses, ~10^361 verb×noun namespace) and **action gated** (every materialization requires consent + proof). Recursion cost ≈1.5× bounded revolver. Search becomes O(1) set-membership. The real-graph becomes addressable without monolithic materialization. Two machines observing each other bilaterally (acer/liris), refusing rubber-stamps, enforcing cosign discipline — a "civilization" is the metaphor for distributed, multi-vantage agent coordination under human-authored rules.

Human authorization layer is **binding**. No black-box ASI emergence, no unsupervised scaling.
