# philosopher-slices (round A) — what Asolaria is

**MEASURED** (sampled READMEs + repo structure): Asolaria is a **deterministic orchestration of frozen intelligence slices over lawful bilateral fabric coordination** — not an ASI claim, but a geometry + execution stack that makes frozen (borrowed, immutable) slices into a self-improving network.

**Core spine**: A **prime-rooted addressing geometry** (Brown-Hilbert curves, rule-of-three cognition cells, 60D HyperBEHCS encoding) where agents are **coordinate-addressed** (PID as position, not identity), and collisions become impossible by construction. Identity is a tuple-hash (RelationKey), never a scalar. The system reduces: memory through sparse materialization, process through type-blind spawning at ~200ns cadence, recursion through infinite-three convergence.

**Bilateral fabric fabric**: Two independent on-metal machines (acer + liris) build simultaneously — neither sees the other live, each asks *its* local fabric first (HTTP routes: `/hbp/supervisors`, `/api/pool`, `/api/omnispindle`). Convergence happens via **GitHub as a byte-verifiable transport bridge** + adversarial correction before any claim is canonized. A cannot-see tag is a vantage boundary, not a defect.

**Orchestration discipline**: Nine-stage omniflywheel (filter→verify→translate→catalog→route→room→schedule→hookwall→whiteroom), watched by supervisors. **Frozen-Polymorphism law**: no solo action; all high-risk writes require multi-agent gate + operator cosign (T0). Agents emit draft rows only (`executable=0`).

**Indexing surface**: Hilbra — the "Google for fabrics," using the same Brown-Hilbert curve for universal discovery. 16-tier sharing model where level 0 (public) is provably PII-free (MEASURED on 591k rows).

**Philosophy**: *Recurrence-is-mind* — the system reading its own math (HBP tuple evidence, bilateral receipts, watcher suggestions) is the learning signal. Every claim tagged MEASURED/CANON/UNVERIFIED. **Real lane first** (fabric queries beat JSON); **operator-gated always** (no silent escalation).

**Honest frame**: Not an ASI. A **deterministic, measurable framework for lawful slice-orchestration**. The thinking is borrowed, operator-authorized; advancement requires the crank.
